galaxy_info:
  role_name: apache
  author: githubfoam
  description: ansible role to deploy apache2
  company: NA
  license: Apache
  min_ansible_version: 2.4
  platforms:
   - name: Ubuntu
     versions:
     - bionic
  galaxy_tags:
    - webserver
    - apache2
    - httpd
dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
